// Generated/maintained Supabase database types.
// Placeholder until Phase 3 defines the full schema — kept minimal so the
// Supabase client generics compile without lying about shape we haven't built yet.
export interface Database {
  public: {
    Tables: Record<string, never>;
    Views: Record<string, never>;
    Functions: Record<string, never>;
    Enums: Record<string, never>;
  };
}
